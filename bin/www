#!/usr/bin/env node
`Javascript harmony features required`;

var debug = require('debug')('todo.website');
var app = require('../app');
require('dotenv').load();

app.set('port', process.env.PORT || 3000);

var server = app.listen(app.get('port'), ()=> {
  debug('Express server listening on port ' + server.address().port);
});
